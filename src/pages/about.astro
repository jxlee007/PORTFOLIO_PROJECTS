---
import BaseLayout from "../layouts/BaseLayout.astro";
import Nav from "../components/Nav.astro";
import Footer from "../components/Footer.astro";
import Split from "../components/Split.astro";
import AboutComponent from "../components/About.astro";
import InfoBlock from "../components/InfoBlock.astro";
import { brandLinks } from "../constants/brandLinks";

// Define menu items for the sidebar (same as index page)
const menuItems = [
  { href: "/", index: "/01", label: "MAIN HOME" },
  { href: "/about", index: "/02", label: "ABOUT JX7" },
  { href: "/work", index: "/03", label: "WORKS" },
  { href: "/contact", index: "/04", label: "CONTACT" },
];

// Capabilities
const capabilities = [
  {
    title: "LANDING PAGES & WEBSITES",
    description:
      "LANDING PAGE DESIGNED TO CONVERT VISITORS INTO ENGAGED USERS EFFORTLESSLY.",
    image: "../images/landing.webp",
    price: "$ 320+",
  },
  {
    title: "WEB APPS",
    description:
      "LEADING THE FOREFRONT OF MOBILE APP PROTOTYPING WITH ADVANCED TECHNOLOGIES.",
    image: "https://sm.pcmag.com/t/pcmag_uk/news/g/google-unw/google-unwraps-android-16-design-details-springier-animation_ze4e.1920.png",
    price: "$ 500+",
  },
  {
    title: "DIGITAL MARKETING AUTOMATION",
    description:
      "UNIQUE BRAND IDENTITIES THAT RESONATE WITH AUDIENCES MEMORABLE CONNECTIONS THROUGH STRATEGIC NARRATIVE FORGING.",
    image: "https://n8niostorageaccount.blob.core.windows.net/n8nio-strapi-blobs-prod/assets/Home_ITO_Ps_5a5aac3fda.webp",
    price: "$ 600+",
  },
];

const images = [
  {
    gif: "https://i.pinimg.com/originals/a7/7f/bf/a77fbfec380b3f63e3feb9a7df60616a.gif",
    text: "AWE SHIT"
  },
  {
    gif: "https://media3.giphy.com/media/v1.Y2lkPTc5MGI3NjExczNwZTdoZ2xxeGM1Mml3czc1NnZzbGNrcmg1d3pxYjg0NHlvOTg5OSZlcD12MV9pbnRlcm5hbF9naWZfYnlfaWQmY3Q9Zw/YbHnru6KfNiUGeNeCf/giphy.gif",
    text: "HERE .WE—GO"
  },
  {
    gif: "https://i.pinimg.com/originals/d8/e9/c4/d8e9c40a8542557a08f6f90745340112.gif",
    text: "AGAIN"
  },
];

const contactDetails = [
  { label: "Mobile", value: "7600925494" },
  { label: "Email", value: "jagmohansinghmalhi20@gmail.com" },
  {
    label: "LinkedIn",
    value: "https://www.linkedin.com/in/jagmohan-singh-malhi-a67805243",
  },
  { label: "GitHub", value: "https://github.com/jxlee007" },
  ,
];
const topSkills = ["No-Code Development Platforms", "AI Agents", "Jira"];

const profileExperience = [
  {
    company: "Kit Solutions",
    position: "IT Business Analyst",
    period: "March 2024 - April 2025",
    location: "Vadodara, Gujarat, India",
  },
  {
    company: "InternPe",
    position: "Web Development Intern",
    period: "November 2023",
    location: "Jaipur, Rajasthan, India",
  },
];

const education = [
  {
    institution: "Parul University",
    degree: "BCA, Cybersecurity",
    period: "August 2021 - April 2024",
    location: "Vadodara, Gujarat, India",
    highlight: "Bachelor of Computer Applications",
    status: "Distinction",
  },
];

const stats = [
  {
    label: "Years of Experience",
    value: "4+",
  },
  {
    label: "Projects",
    value: "10+",
  },
  {
    label: "Creative Potential",
    value: "♾️",
  },
  {
    label: "Customer Satisfaction",
    value: "5.00",
  },
];

const stackdata = [
  {
    "id": 1,
    "icon": "/icons/framer.svg",
    "name": "LOVABLE",
    "subtitle": "NO CODE TOOL",
    "percent": 99,
    "index": "[1]"
  },
  {
    "id": 2,
    "icon": "/icons/figma.svg",
    "name": "FIGMA",
    "subtitle": "AI PROTOTYPING TOOL",
    "percent": 88,
    "index": "[2]"
  },
  {
    "id": 3,
    "icon": "/icons/chatgpt.svg",
    "name": "CLAUDE",
    "subtitle": " AI ASSISTANCE",
    "percent": 100,
    "index": "[3]"
  },
  {
    "id": 4,
    "icon": "/icons/midjourney.svg",
    "name": "MIXBOARD",
    "subtitle": "AI IMAGE GENERATION",
    "percent": 96,
    "index": "[4]"
  },
  {
    "id": 5,
    "icon": "/icons/notion.svg",
    "name": "ANTIGRAVITY(VS-CODE)",
    "subtitle": "PRODUCTIVITY PLATFORM",
    "percent": 91,
    "index": "[5]"
  },
  {
    "id": 6,
    "icon": "/icons/react.svg",
    "name": "REACT/ASTRO/NEXT-JS",
    "subtitle": "JAVASCRIPT UI LIBRARY",
    "percent": 94,
    "index": "[6]"
  },
  {
    "id": 7,
    "icon": "/icons/midjourney.svg",
    "name": "CLERK/CONVEX",
    "subtitle": "REALTIME CLOUD AUTH & DATABASE",
    "percent": 96,
    "index": "[7]"
  },
  {
    "id": 8,
    "icon": "/icons/tailwind.svg",
    "name": "TAILWIND",
    "subtitle": "CSS RESPONSIVE-NESS",
    "percent": 90,
    "index": "[8]"
  },
]


---

<BaseLayout title="About | JX7 Portfolio">
  <Nav {menuItems} {brandLinks} />

  <!-- Header -->
  <section class="mt-10 pb-6 md:pb-10 px-4 md:px-0">
    <div
      class="font-bold font-space-grotesk w-full gap-6 md:gap-10 flex flex-col items-center justify-center"
    >
      <span class="text-center">
        <Split text="MORE ABOUT " />
        <Split text="JXLEE" />
      </span>

      <p
        class="w-full md:w-2/3 text-zinc-400 leading-relaxed text-xl md:text-3xl mt-4 text-center tracking-wide font-light px-4 md:px-0"
      >
        UNLEASH YOUR CREATIVITY. SHOWCASE YOU WORK AND
        <br class="hidden md:block" />
        HIGHLIGHT YOUR UNIQUE SKILLS TO ATTRACT NEW CLIENTS.
      </p>
    </div>
  </section>

  <!-- Statistics -->
  <section class="px-4 md:px-0 py-6 md:py-10">
    <div
      class="flex flex-col md:flex-row items-center md:items-center justify-around gap-8 md:gap-6 w-full"
    >
      {
        stats.map((stat) => (
          <div class="flex-1 flex flex-col items-center animate-item opacity-0 translate-y-6 will-change-transform">
            <span class="text-5xl md:text-7xl lg:text-6xl font-bold font-space-grotesk text-white">
              {stat.value}
            </span>
            <span class="text-zinc-400 uppercase tracking-widest text-md mt-2 text-center md:text-left">
              {stat.label}
            </span>
          </div>
        ))
      }
    </div>
  </section>

  <div class="w-full px-4 mt-10 flex flex-col items-end">
    <div
      class="mt-10 w-full h-full grid grid-flow-row justify-items-end gap-4 md:gap-6"
    >
      <InfoBlock class="animate-item opacity-0 translate-y-6 will-change-transform" title="ABOUT JX7">
        I'm on the cutting edge of no-code tools that allow me to bring my
        creative visions to life. Though my methods may be unconventional, my
        dedication to the craft is unparalleled. I thrive on finding "unexpected
        solutions" and believe that with the right perspective.
      </InfoBlock>
    </div>

    <div
      class="mt-10 w-full h-full grid grid-flow-row justify-items-end gap-4 md:gap-6"
    >
      <InfoBlock class="animate-item opacity-0 translate-y-6 will-change-transform" title="MY APPROACH & STRATEGY">
        I take a pragmatic, design-first approach — rapid code & AI prototyping
        to validate ideas, then polish with hand-crafted HTML/Tailwind/JS when
        it matters. I prioritize clear UX, automation that saves time, and fast
        iteration through collaboration and curiosity. Always down to build
        smart, beautiful experiences together.
      </InfoBlock>
    </div>
  </div>

  <!-- Hero Image -->
  <div class="mt-20 w-full mx-auto animate-item opacity-0 translate-y-6 will-change-transform">
    <div class="relative overflow-hidden h-96 rounded-2xl shadow-lg bg-zinc-900">
      <!-- square on mobile, horizontal rectangle on md+ -->
      <div class="relative w-full  pt-[100%] md:pt-[50%]">
        <img
          src="../images/vc3pp7130gr71.webp"
          alt="Wide about hero"
          class="absolute inset-0 w-full h-96 object-cover"
        />
      </div>
    </div>
  </div>

  <!-- Experience Section -->
  <div class="my-24" id="experience">
    <div
      class="gallery-experience w-full flex flex-col lg:flex-row justify-around sm:gap-20 "
    >
      <!-- Left: Experience heading pinned on large screens -->
      <div
        class=" w-full lg:w-1/3 order-1 lg:order-1 flex items-start justify-start"
      >
        <h2
          id="experience-headings"
          class="text-5xl md:text-4xl lg:text-5xl font-bold tracking-wide mb-8 uppercase lg:mb-0 lg:min-w-[220px] animate-item opacity-0 translate-y-6 will-change-transform"
        >
          EXPERIENCE
        </h2>
      </div>

      <!-- Right: Experience cards -->
      <div
        id="experience-cards"
        class="w-full order-2 lg:order-2 flex flex-col gap-6"
      >
        {
          profileExperience.map((exp, index) => (
            <article
              class="experience-card relative flex flex-col gap-6 p-6 md:p-8 shadow-[0_18px_45px_-25px_rgba(0,0,0,0.65)] transform-gpu hover:-translate-y-2 hover:border-sky-400/60 hover:bg-zinc-900/90 focus-visible:outline focus-visible:outline-sky-400/60 animate-item opacity-0 translate-y-6 will-change-transform"
              style={`transition: transform 550ms linear(0, 0.1753, 0.5154, 0.8278, 1.0297, 1.1163, 1.1211, 1.0857, 1.0422, 1.0084, 0.9897, 0.9841, 0.9864, 0.9917, 0.9968, 1.0002, 1.0018, 1), box-shadow 220ms ease, background-color 220ms ease, border-color 220ms ease;`}
            >
              <div class="flex flex-col md:flex-row items-start justify-between gap-4">
                <div class="flex flex-col  gap-3">
                  <span class="text-sm font-semibold tracking-[0.35em] text-sky-400/80">
                    {/* {String(index + 1).padStart(2, "0")} */}
                  </span>
                  <h3 class="text-2xl md:text-3xl font-semibold uppercase tracking-wide">
                    {exp.company}
                  </h3>
                </div>
                <span class="inline-flex items-center py-1 text-xs font-medium uppercase tracking-[0.25em] text-sky-300">
                  {exp.period}
                </span>
              </div>

              <div class="flex flex-col gap-3 text-left">
                <p class="text-lg font-medium uppercase tracking-[0.2em] text-sky-200">
                  {exp.position}
                </p>
                <p class="text-zinc-400 text-base md:text-lg leading-relaxed uppercase tracking-wide">
                  {exp.location}
                </p>
              </div>

            </article>
          ))
        }

        {
          education.map((edu) => (
            <article
              class="experience-card relative flex flex-col gap-6  bg-zinc-900/60 p-6 md:p-8 shadow-[0_18px_45px_-25px_rgba(0,0,0,0.65)] transform-gpu hover:-translate-y-2 hover:border-sky-400/60 hover:bg-zinc-900/90 focus-visible:outline focus-visible:outline-sky-400/60 animate-item opacity-0 translate-y-6 will-change-transform"
              style={`transition: transform 550ms linear(0, 0.1753, 0.5154, 0.8278, 1.0297, 1.1163, 1.1211, 1.0857, 1.0422, 1.0084, 0.9897, 0.9841, 0.9864, 0.9917, 0.9968, 1.0002, 1.0018, 1), box-shadow 220ms ease, background-color 220ms ease, border-color 220ms ease;`}
            >
              <div class="flex flex-col md:flex-row items-start justify-between gap-4">
                <div class="flex flex-col  gap-3">
                  {/* <span class="text-sm font-semibold tracking-[0.35em] text-sky-400/80">
                    EDU
                  </span> */}
                  <h3 class="text-2xl md:text-3xl font-semibold uppercase tracking-wide">
                    {edu.institution}
                  </h3>
                </div>
                <span class="inline-flex items-center py-1 text-xs font-medium uppercase tracking-[0.25em] text-sky-300">
                  {edu.period}
                </span>
              </div>

              <div class="flex flex-col gap-3 text-left">
                <p class="text-lg font-medium uppercase tracking-[0.2em] text-sky-200">
                  {edu.degree}
                </p>
                <p class="text-zinc-400 text-base md:text-lg leading-relaxed uppercase tracking-wide">
                  {edu.location}
                </p>
              </div>

              <div class="flex items-center justify-between text-[0.7rem] md:text-xs tracking-[0.35em] uppercase text-zinc-500">
                <span class="flex items-center gap-2">
                  <span class="h-2.5 w-2.5 rounded-full bg-sky-400 shadow-[0_0_12px_rgba(56,189,248,0.6)]"></span>
                  {edu.highlight}
                </span>
                <span class="font-medium text-zinc-100/80">
                  {edu.status}
                </span>
              </div>
            </article>
          ))
        }

        <!-- shared end marker used by ScrollTrigger to calculate pin duration -->
        <hr id="experience-end" class="border border-transparent mt-2" />
      </div>
    </div>
  </div>

  <!-- My Stack Section -->
  <div class="my-24" id="my-stack">
    <div
      class="gallery-stack w-full flex flex-col lg:flex-row justify-around gap-20"
    >
      <!-- Left: My Stack heading (now on the left and will be pinned on large screens) -->
      <div
        class="md:mr-10 w-full md:w-1/3 order-1 lg:order-1 flex items-start justify-start"
      >
        <h2
          id="my-stack-heading"
          class="text-5xl md:text-4xl lg:text-5xl font-bold tracking-wide mb-8 uppercase lg:mb-0 lg:min-w-[220px] animate-item opacity-0 translate-y-6 will-change-transform"
        >
          MY STACK
        </h2>
      </div>

      <!-- Right: Stack cards (moved to the right) -->
      <div  id="my-stack-cards" class="w-full order-2 lg:order-2 flex flex-col gap-6">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            {stackdata.map(card => (
              <div class="relative bg-gradient-to-b from-slate-800 to-black shadow-lg p-6 min-h-[180px] hover:bg-zinc-800 transition-colors duration-300 animate-item opacity-0 translate-y-6">
                <div class="flex justify-between items-start mb-2">
                  <img src={card.icon} alt={card.name + ' Icon'} class="w-8 h-8" />
                  <span class="text-white/60 text-lg tracking-widest absolute right-6 top-6">{card.index}</span>
                </div>
                <h3 class="text-white font-bold text-2xl tracking-wider uppercase mb-2">{card.name}</h3>
                <p class="text-zinc-400 font-mono text-base tracking-wide">{card.subtitle}</p>
                <span class="absolute right-6 bottom-6 text-white/90 text-xl font-semibold">{card.percent}%</span>
            </div>
          ))}
        </div>
      </div>
    </div>

    <!-- end marker for GSAP pin end -->
    <hr id="my-stack-end" class="border border-transparent mt-6" />
  </div>

  <section class="w-full px-4 md:px-0">
    <div class="flex flex-col lg:flex-row gap-6 items-end">
      <!-- Left box -->
      <div
        class="w-full lg:w-96 order-2 lg:order-1 shadow-lg flex flex-col animate-item opacity-0 translate-y-6 will-change-transform"
      >

      <p class=" uppercase text-white/80 leading-8 tracking-tight text-lg "> 
        A creator crafts visual narratives that blend creativity with
        functionality, shaping impactful experiences through artful design.
      </p>

      </div>
      <!-- Right box -->
      <div
        class="w-full flex justify-end order-1 lg:order-2 leading-tight md:leading-none font-space-grotesk  animate-item opacity-0 translate-y-6 will-change-transform"
      >
      <span class=" inline-block w-[90%] sm:w-full ">  
        <p class="font-semibold text-5xl md:text-8xl text-white text-right tracking-normal uppercase line-clamp-3 ">
          AWE MAKES THINGS HAPPEN .wo—nder
        </p>
      </span>
      </div>
    </div>

    <!-- GIF Section -->
    <section class="my-8 w-screen md:w-full -mx-4 md:mx-0">
      <div class="grid grid-cols-1 lg:grid-cols-3 gap-4">
        {
          images.slice(0, 3).map((img) => (
            <div class="group relative overflow-hidden rounded-lg animate-item opacity-0 translate-y-6 will-change-transform" style="padding-top:100%;">
              <img
                src={img.gif}
                alt="gif"
                class="absolute inset-0 w-full h-full object-cover"
              />

              <!-- overlay: visible on lg+, hides on hover -->
              <div
                class="absolute inset-0 hidden lg:flex items-center justify-center px-4 text-center bg-gradient-to-t from-gray-800 via-gray-700 to-gray-600 text-white transition-opacity duration-300 lg:opacity-100 lg:group-hover:opacity-0"
              >
                <p class="max-w-xs">
                  <span class="flex flex-col items-center">
                    <span class="w-16 h-px bg-white/20 mb-4" aria-hidden></span>
                    {
                      img.text.split(' ').map((word) => (
                        <span class="block text-5xl md:text-7xl font-bold uppercase tracking-wider bg-gradient-to-t from-gray-400 via-gray-300 to-gray-200 bg-clip-text text-transparent">
                          {word}
                        </span>
                      ))
                    }
                  </span>
                </p>
              </div>
            </div>
          ))
        }
      </div>
    </section>

    <!-- Capabilities -->
    <div class="my-14" id="capabilities">
      <div
        class="gallery-capabilities w-full flex flex-col lg:flex-row justify-between gap-8"
      >
        <!-- Left: Capabilities heading (pinned on large screens) -->
        <div class="  w-full lg:w-1/3 order-1 lg:order-1 flex items-start">
          <h2
            id="capabilities-heading"
            class="text-5xl md:text-4xl lg:text-5xl font-bold tracking-wide mb-8 uppercase lg:mb-0 lg:min-w-[220px] animate-item opacity-0 translate-y-6 will-change-transform"
          >
            CAPABILITIES
          </h2>
        </div>

        <!-- Right: capability cards -->
        <div
          id="capabilities-cards"
          class="w-full order-2 lg:order-2 flex flex-col gap-6"
        >
          {
            capabilities.map((cap, index) => (
              <section
                class="capability-section capability-card relative flex flex-col gap-6 p-6 md:p-8 shadow-[0_18px_45px_-25px_rgba(0,0,0,0.65)] transform-gpu hover:-translate-y-2 hover:border-emerald-400/60 hover:bg-zinc-900/90 focus-visible:outline focus-visible:outline-emerald-400/60 animate-item opacity-0 translate-y-6 will-change-transform"
                style={`transition: transform 550ms linear(0, 0.1753, 0.5154, 0.8278, 1.0297, 1.1163, 1.1211, 1.0857, 1.0422, 1.0084, 0.9897, 0.9841, 0.9864, 0.9917, 0.9968, 1.0002, 1.0018, 1), box-shadow 220ms ease, background-color 220ms ease, border-color 220ms ease;`}
              >
                  <div class=" size-10 ">
                    <img src="../images/icon.webp" alt="icon">
                  </div>
                <div class="flex items-start justify-between gap-4">

                  <div class="flex flex-row gap-6">
                    <h3 class="text-xl md:text-3xl font-semibold uppercase tracking-tight">
                      {cap.title}
                    </h3>
                    <span class="inline-flex items-center bg-emerald-500/10 px-3 py-1 text-sm font-medium uppercase tracking-[0.25em] text-emerald-300">
                      {cap.price}
                    </span>
                  </div>
                </div>

                <p class="text-zinc-400 text-lg leading-8 uppercase tracking-tight">
                  {cap.description}
                </p>

                <div class="w-full h-60 rounded-lg overflow-hidden mb-4">
                  <img
                    src={cap.image}
                    alt={cap.title + " Image"}
                    class="w-full h-full object-cover bg-red-500 "
                  />
                </div>

                <div class="flex items-center justify-between text-xs md:text-sm tracking-[0.3em] uppercase text-zinc-500">
                  <span class="flex items-center gap-2">
                    <span class="h-2.5 w-2.5 rounded-full bg-emerald-400 shadow-[0_0_12px_rgba(16,185,129,0.6)]"></span>
                    Custom Sprint Ready
                  </span>
                  <span class="font-medium text-zinc-100/80">
                    Delivery: 2-4 Weeks
                  </span>
                </div>
              </section>
            ))
          }
        </div>
      </div>

      <!-- end marker for GSAP pin/end -->
      <hr id="capabilities-end" class="border border-transparent mt-6" />
    </div>
  </section>

  <!-- inline GSAP reveal: subtle on-scroll animations for .animate-item -->
  <script is:inline>
    document.addEventListener('DOMContentLoaded', () => {
      // small delay so GSAP/ScrollTrigger (if loaded) has a chance to register
      setTimeout(() => {
        const items = Array.from(document.querySelectorAll('.animate-item'));
        if (!items.length) return;

        // If GSAP + ScrollTrigger available, use it for smoother reveals
        if (window.gsap && window.ScrollTrigger) {
          const gsap = window.gsap;
          const ScrollTrigger = window.ScrollTrigger;
          try {
            gsap.registerPlugin(ScrollTrigger);
          } catch (e) {
            // plugin may already be registered
          }

          // ensure all ScrollTrigger instances use the locomotive scroller
          if (typeof ScrollTrigger.defaults === "function") {
            ScrollTrigger.defaults({ scroller: "#main" });
          }

          items.forEach((el, i) => {
            // ensure starting state (in case CSS classes removed)
            gsap.set(el, { autoAlpha: 0, y: 18 });
            gsap.to(el, {
              autoAlpha: 1,
              y: 0,
              duration: 0.6,
              ease: 'power2.out',
              delay: 0,
              scrollTrigger: {
                trigger: el,
                start: 'top 85%',
                scroller: '#main',
                toggleActions: 'play none none reverse',
                markers: false,
              },
            });
          });
          ScrollTrigger.refresh();
          return;
        }

        // Fallback (no GSAP): simply remove hidden classes for native appearance
        items.forEach((el) => {
          el.classList.remove('opacity-0', 'translate-y-6');
        });
      }, 120);
    });
  </script>
  <AboutComponent />

  <Footer />

  <script is:inline>
    document.addEventListener("DOMContentLoaded", () => {
      // small delay to allow other scripts/hydration to finish
      setTimeout(() => {
        if (!(window.gsap && window.ScrollTrigger)) return;
        const gsap = window.gsap;
        const ScrollTrigger = window.ScrollTrigger;
        const mm = gsap.matchMedia();
        // keep references to created ScrollTriggers for cleanup
        const created = [];

        const capabilityCards = Array.from(
          document.querySelectorAll(".capability-card"),
        );
        const experienceCards = Array.from(
          document.querySelectorAll(".experience-card"),
        );

        const createHighlightTriggers = (cards) => {
          if (!cards.length) return;
          const setActive = (target) => {
            cards.forEach((card) =>
              card.classList.toggle("is-active", card === target),
            );
          };

          // activate first card by default
          setActive(cards[0]);

          cards.forEach((card) => {
            ScrollTrigger.create({
              trigger: card,
              scroller: "#main",
              start: "top 70%",
              end: "bottom 40%",
              onEnter: () => setActive(card),
              onEnterBack: () => setActive(card),
              onLeave: () => card.classList.remove("is-active"),
              onLeaveBack: () => card.classList.remove("is-active"),
              markers: false,
            });
          });
        };

        createHighlightTriggers(capabilityCards);
        createHighlightTriggers(experienceCards);

        // Desktop: pins and image swap behavior
        mm.add("(min-width: 1024px)", () => {
          // EXPERIENCE heading pin
          const experienceHead = document.querySelector("#experience-headings");
          const experienceEnd = document.querySelector("#experience-end");
          if (experienceHead && experienceEnd) {
            created.push(
              ScrollTrigger.create({
                trigger: experienceHead,
                scroller: "#main",
                start: "top top+=20%",
                endTrigger: experienceEnd,
                end: "bottom top+=80%",
                pin: experienceHead,
                pinSpacing: false,
                anticipatePin: 1,
                markers: false,
              }),
            );
          }

          // CAPABILITIES heading pin
          const capabilitiesSection = document.querySelector("#capabilities");
          const capabilitiesHeading = document.querySelector("#capabilities-heading");
          const capabilitiesEnd = document.querySelector("#capabilities-end");
          if (capabilitiesSection && capabilitiesHeading && capabilitiesEnd) {
            created.push(
              ScrollTrigger.create({
                trigger: capabilitiesSection,
                scroller: "#main",
                start: "top top+=120",
                endTrigger: capabilitiesEnd,
                end: "bottom top+=80%",
                pin: capabilitiesHeading,
                pinSpacing: false,
                anticipatePin: 1,
                markers: false,
              }),
            );
          }

          // MY STACK heading pin
          const myStackHeading = document.querySelector("#my-stack-heading");
          const myStackEnd = document.querySelector("#my-stack-end");
          const myStack = document.querySelector("#my-stack");
          if (myStack && myStackHeading && myStackEnd) {
            created.push(
              ScrollTrigger.create({
                trigger: myStack,
                scroller: "#main",
                start: "top top+=120",
                endTrigger: myStackEnd,
                end: "bottom top+=80%",
                pin: myStackHeading,
                pinSpacing: false,
                anticipatePin: 1,
                markers: false,
              }),
            );
          }

          // cleanup for this media query
          return () => {
            created.forEach((t) => t && t.kill && t.kill());
            created.length = 0;
          };
        });

        ScrollTrigger.refresh();
      }, 250);
    });
  </script>
</BaseLayout>