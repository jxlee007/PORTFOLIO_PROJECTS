---
import MenuLink from './MenuLink.astro';
import CustomLink from './CustomLink.astro';

interface MenuItem {
  href: string;
  index: string;
  label: string;
}

interface BrandLink {
  href: string;
  text: string;
}

export interface Props {
  menuItems: MenuItem[];
  brandLinks: BrandLink[];
}

const { menuItems, brandLinks } = Astro.props;
---
<div id="sidebar" class="sidenav h-screen w-0 fixed top-0 right-0 overflow-x-hidden transition-all duration-500 pt-[60px] bg-black z-[9999]">
  <div class="flex justify-between items-center mx-8 border rounded-full border-white-800">
    <marquee class="text-lg font-semibold" direction="left" behavior="scroll" loop="infinite" scrollamount="10">
      <span class="text-gray-400 mr-2">+</span> LIGHT MODE COMING SOON <span class="text-gray-400 ml-2">+</span>
    </marquee>
    <button type="button" class="closebtn text-white text-2xl hover:text-gray-400 transition-colors duration-300" onclick="closeNav()">
      <span class="px-6 py-6 pl-2 border border-white hover:bg-white hover:text-black transition-all duration-300">CLOSE</span>
    </button>
  </div>

  <div id="reload" class="mt-16 px-8">
    {menuItems.map(item => (
      <MenuLink href={item.href} index={item.index} label={item.label} />
    ))}
  </div>

  <div id="brand" class="absolute text-lg bottom-10 left-0 w-full px-8 flex justify-between">
    {brandLinks.map(link => (
      <CustomLink href={link.href} underline={true} className="text-gray-400 hover:text-white transition-colors duration-300">
        <span>{link.text}</span>
      </CustomLink>
    ))}
  </div>
</div>