---
import CustomLink from './CustomLink.astro';

export interface Props {
  href: string;
  target?: string;
}

const { href, target } = Astro.props;
---
<CustomLink 
  {href} 
  {target}
  className="border-white border-2 rounded-full w-fit h-fit px-4 md:px-5 py-2 md:py-3 text-base md:text-lg font-semibold text-white duration-300 hover:bg-white hover:text-black"
>
  <slot />
</CustomLink>